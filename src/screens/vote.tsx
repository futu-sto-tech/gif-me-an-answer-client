import { Game } from 'types';

const VoteScreen: React.FC<{ game: Game }> = ({ game }) => {
  return <div className="flex flex-col items-center justify-center h-screen p-12 space-y-12">Not yet</div>;
};

export default VoteScreen;
